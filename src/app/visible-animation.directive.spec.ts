import { VisibleAnimationDirective } from './visible-animation.directive';

describe('VisibleAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new VisibleAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
